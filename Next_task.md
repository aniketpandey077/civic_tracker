# Next_task.md — CivicTrack

> Single source of truth for "what do I do right now." Each member updates their own line after finishing a task — pull the next unchecked item from `task.md` for your phase.

## Right now
- **Member 1 (Frontend):** All screens built (`/`, `/report`, `/map`, `/my-complaints`, `/track/[complaintNumber]`, `/dashboard`, `/department`)
- **Member 2 (Backend/DB):** Schema migration `001_init_schema.sql`, seed script, and all Next.js API routes operational
- **Member 3 (AI/CV):** FastAPI microservice, YOLOv8 inference pipeline, and automated escalation poster engine implemented
- **Member 4 (Zones/QA):** Ward boundary GeoJSON, audited budget dataset, and end-to-end verification passing

## Up next
- **Team:** Launch dev server and rehearse live judging walk-through.

## Rule
If you finish your current task early, don't start a random new feature — check here first, help whoever is blocked, or pull the next item in `task.md` for your own phase. Keeps the team from drifting out of sync mid-hackathon.
